//Sabrina Jackson
using UnityEngine;

//allows slot to be selected while board ignores raycast, gives access to slot number and if store
public class SlotSelected : MonoBehaviour
{
    public int slotNumber;
    public bool isaStore;
}
